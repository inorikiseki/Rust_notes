# rustc
rustc main.rc   ;compile your source
# cargo 
cargo new
cargo build
cargo run
cargo check

##  cargo build --release
## work on an existing project 
$ git clone exmple.org/someproject
$ cd someproject
$cargo build
